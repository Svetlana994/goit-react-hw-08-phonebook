import { Container } from "../App.styles";

function HomeView() {
  return (
    <Container>
      <h1>
        This program is a simple application that you can find on your mobile as
        a contacts app. This allows the users to save contacts under a name. The
        Phonebook application contains a set of basic functions for adding,
        searching, updating, and deleting new contacts.
      </h1>
    </Container>
  );
}

export default HomeView;
